'use strict';

var config = require('../config/config');

// exports.extractPidFromUrl = function(url) {
	
// 	var pidIndex = url.indexOf(config.institutionPrefix), len = url.length;
// 	var pid = url.slice(pidIndex, len);
// 	return pid;
// }